flowchart LR
    %% DataFlow: BroadcastFlow

    source(["source"])
    broadcast[/"broadcast"/]
    validator["validator"]
    archiver["archiver"]
    notifier["notifier"]

    source -->|Int32| broadcast
    broadcast -->|Int32| validator
    broadcast -->|Int32| archiver
    broadcast -->|Int32| notifier

    %% 'broadcast' broadcasts to multiple targets
