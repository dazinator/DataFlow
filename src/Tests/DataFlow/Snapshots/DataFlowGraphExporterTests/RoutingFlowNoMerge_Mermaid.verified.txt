flowchart LR
    %% DataFlow: RoutingFlowNoMerge

    source(["source"])
    router["router"]

    source -->|Int32| router

    %% Routes for 'router':
    subgraph router_route_even ["Route: even"]
        direction LR
        even_transform[/"[ENTRY] even-transform<br/>Int32 → String"/]
        even_processor["even-processor<br/>String →"]

        even_transform -->|String| even_processor
    end

    router -.->|Int32<br/>'even'| router_route_even
    subgraph router_route_odd ["Route: odd"]
        direction LR
        odd_transform[/"[ENTRY] odd-transform<br/>Int32 → String"/]
        odd_processor["odd-processor<br/>String →"]

        odd_transform -->|String| odd_processor
    end

    router -.->|Int32<br/>'odd'| router_route_odd
